{
  "name": "puppeteer-image-base64-api",
  "version": "1.0.0",
  "description": "A Puppeteer microservice to convert images to base64 using a headless browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.3.8"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
